<UserControl x:Class="Dear_Doctor.Controls.AutoCompleteTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="300">
    <Grid>
        <TextBox x:Name="InputTextBox" 
                 Style="{StaticResource PremiumTextBoxStyle}"
                 TextChanged="InputTextBox_TextChanged"
                 PreviewKeyDown="InputTextBox_PreviewKeyDown"
                 LostFocus="InputTextBox_LostFocus"/>
        
        <Popup x:Name="SuggestionsPopup"
               PlacementTarget="{Binding ElementName=InputTextBox}"
               Placement="Bottom"
               IsOpen="False"
               StaysOpen="False"
               AllowsTransparency="True"
               PopupAnimation="Fade">
            <Border Background="#FFFFFF"
                    BorderBrush="#CBD5E1"
                    BorderThickness="1"
                    CornerRadius="6"
                    Width="{Binding ElementName=InputTextBox, Path=ActualWidth}"
                    MaxHeight="200"
                    Margin="0,2,0,10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Color="#000000" Opacity="0.1" ShadowDepth="3" Direction="270"/>
                </Border.Effect>
                <ListBox x:Name="SuggestionsListBox"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         SelectionChanged="SuggestionsListBox_SelectionChanged"
                         PreviewKeyDown="SuggestionsListBox_PreviewKeyDown">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="ItemBorder" Padding="8,6" Background="Transparent" CornerRadius="4">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ItemBorder" Value="#F1F5F9" Property="Background"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Value="#E2E8F0" Property="Background"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="2">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" Foreground="#0F172A"/>
                                <WrapPanel Margin="0,1,0,0">
                                    <TextBlock Text="{Binding GenericName}" FontSize="11" Foreground="#64748B" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding DefaultDose}" FontSize="11" Foreground="#0F766E" FontWeight="Medium"/>
                                </WrapPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Popup>
    </Grid>
</UserControl>
