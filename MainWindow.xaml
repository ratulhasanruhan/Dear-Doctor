<Window x:Class="Dear_Doctor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dear_Doctor"
        mc:Ignorable="d"
        Title="Dear Doctor - Prescription Generator" 
        Height="768" Width="1280" 
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC"
        PreviewKeyDown="Window_PreviewKeyDown">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/> <!-- Navigation Sidebar -->
            <ColumnDefinition Width="*"/>   <!-- Main Content Panel -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#0F172A" Padding="16,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Logo/Branding -->
                    <RowDefinition Height="*"/>    <!-- Navigation Options -->
                    <RowDefinition Height="Auto"/> <!-- Footer Details -->
                </Grid.RowDefinitions>

                <!-- Branding Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,32">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="10" Height="24" Background="#14B8A6" CornerRadius="3" Margin="0,0,10,0"/>
                        <TextBlock Text="Dear Doctor" FontSize="20" FontWeight="ExtraBold" Foreground="#FFFFFF" FontFamily="{StaticResource MainFont}"/>
                    </StackPanel>
                    <TextBlock Text="Prescription Suite" FontSize="11" Foreground="#64748B" Margin="20,4,0,0" FontWeight="SemiBold"/>
                </StackPanel>

                <!-- Navigation List -->
                <StackPanel Grid.Row="1">
                    <!-- Prescription Generator Tab -->
                    <RadioButton x:Name="PrescriptionTab"
                                 Content="Prescription Generator"
                                 Style="{StaticResource SidebarButtonStyle}"
                                 IsChecked="True"
                                 Checked="NavigationTab_Checked"
                                 Margin="0,0,0,8"/>
                    
                    <!-- Medicine Database Tab -->
                    <RadioButton x:Name="MedicinesTab"
                                 Content="Medicine Database"
                                 Style="{StaticResource SidebarButtonStyle}"
                                 Checked="NavigationTab_Checked"/>
                </StackPanel>

                <!-- Sidebar Footer -->
                <StackPanel Grid.Row="2" Margin="0,20,0,0">
                    <Border Height="1" Background="#1E293B" Margin="0,0,0,12"/>
                    <TextBlock Text="Dear Doctor v1.0" FontSize="11" Foreground="#475569" HorizontalAlignment="Center"/>
                    <TextBlock Text="Offline Local Storage" FontSize="10" Foreground="#475569" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Column="1" Padding="0">
            <ContentControl x:Name="MainContentFrame" Focusable="False"/>
        </Border>
    </Grid>
</Window>
